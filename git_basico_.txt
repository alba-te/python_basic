configuracion de GIT 
nombre de usuario
git confit-global --global user.name "nombre"
direccion email
git config --global user.email "email"gmail.com"

configurar el editor del codigo VSCgit config --global care.editor..

configurar el editor de codigo VSC
git config --global core.editor "code --wait"
correcion el salto de linea 
git config --global core.autocrlf input
para ver la configuaracon actual 
git config --global -a








iniciamos el repositorio (solo al empezar )


para ver el estado del repositorio
git status

para pasar a la fase stage 
gt add . 
para pasar a commit (ficheros validados) para que pase a comit tiene que estar en la otra fase 
git add .

git remote add origin https://github.com/alba-te/python_basic.git(solo una vez)
git branch -M main (solo una vez)
git push -u origin main
git add .









