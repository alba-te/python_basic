"""
CARACTERISTICAS BASICAS DE PYTHON
"""
#VARIABLES
    #Una variable es una espacio en memoria 
    #para guardar datos , por lo tanto es un contenedor

    #Para crear variables:
        #identificador =  valor
        #Hay reglas para llamar a los identificadores = nombre de la variable
        #Que no PODEMOS hacer?
            #1variable (empezar por un numero)
            #variable (ni empezar nicontener simbolos especiales)
            #de estos errores nos avisará VSC
        #Que no DEBEMOS hacer:
            #contener caracteres fuera del idioma ingles, 
            #como ñ, ç,á,ö,ejemplo_comillas_triple
            #empezar por guion bajo ( reservado para distintas ocasiones)
        #Que debemos hacer?
            #nombrar a nuestras variables con palabras descriptivas
            #podemos usar más de una palabra separadas por un guion bajo (directiva PEP-8)

#control ñ abrir terminal en visual
#como saber si el git está instalado 
#instalar git
# si se me sale voy a la carpeta y pon¡ngo code .
#configuracion git sofeware que permite gestionar como
# nuestro proyecto se va modificando: git config --global user.name alba
#git config --global user.email albatallon01@gmail.com
# cuando lo subaos a git hub le quitara el salto de linea qu ele ha dado windows y
#  cuando te lo quietes se quitara el salto de linea
#si no estas en windows detras ponemos imput
# git config --global core.editor "code --wait" wait bloqueara el sitema hasta
#  que cierres la pestña y el sistema volvera a la normalidad







